<%= form_for(@user_assessment) do |f| %>
  <% if @user_assessment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_assessment.errors.count, "error") %> prohibited this user_assessment from being saved:</h2>

      <ul>
      <% @user_assessment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
